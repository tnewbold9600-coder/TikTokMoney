TIKTOK MONEY V3 — AI TEAM

WHAT WORKS IMMEDIATELY
- One-screen dashboard
- Product scoring + ranked queue
- AI Operations Feed with real local events
- Six visible agent roles
- Draggable persistent AI Team chat bubble
- Boss command history
- Performance feedback loop
- Local shared memory / JSON export

LIVE AI CHAT
The included Netlify Function /api/team becomes live when OPENAI_API_KEY is set
as a secret Netlify environment variable. Optional OPENAI_MODEL can override
the default model.

IMPORTANT
The UI never fabricates web searches. True independent agent web research needs
a search-capable backend/provider/tool integration; V3 intentionally labels the
team idle/unavailable rather than faking activity.

Deploy this entire folder/ZIP so the netlify/functions directory is included.
